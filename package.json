{
  "name": "portfolio_nextjs",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://muhdzaheermv.github.io/portfolio",
  "scripts": {
    "dev": "next dev",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider next build && next export && echo > out/.nojekyll",
    "start": "next --openssl-legacy-provider start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d out"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "next": "10.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-icons": "^4.2.0",
    "styled-components": "^5.3.0",
    "styled-normalize": "^8.0.7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
